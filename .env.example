# ===========================================
# Portfolio — single source of truth (dev)
# Copy to .env in repo root for docker compose
# NEVER commit real values
# ===========================================

# Backend (server-only, never VITE_)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/free
RESEND_API_KEY=
RESEND_TEMPLATE_ID=
FROM_EMAIL=Acme <onboarding@resend.dev>
CONTACT_EMAIL=contacto@juansasia.com
SECRET_KEY=change-me-min-32-chars
CLIENT_ID=portfolio-client
CLIENT_SECRET=change-me
ENV=dev

# Frontend (public, baked at build time)
VITE_API_URL=/api
