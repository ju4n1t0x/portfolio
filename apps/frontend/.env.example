# ===========================================
# Frontend Environment Variables
# ===========================================
# Copy to .env.local (dev). Prod build uses Docker ARG.
# NEVER commit actual values. NEVER put secrets here:
# everything VITE_* is baked into the public JS bundle.

# Backend API URL (same-origin via nginx proxy in prod)
VITE_API_URL=/api
